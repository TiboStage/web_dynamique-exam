<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}StoryForge{% endblock %}</title>
    
    <meta name="description" content="{% block meta_description %}StoryForge - Plateforme collaborative de création narrative{% endblock %}">
    
    {# Favicon #}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>📖</text></svg>">
    
    {# Bootstrap Icons #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    {# Webpack Encore CSS #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body>
    {# Navigation #}
    {% block navbar %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
        <div class="container">
            <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
                <span class="brand-icon me-2">📖</span>
                StoryForge
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" 
                    aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.get('_route') == 'app_home' %}active{% endif %}" 
                           href="{{ path('app_home') }}">
                            <i class="bi bi-house-door me-1"></i> Accueil
                        </a>
                    </li>
                    
                    {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link {% if 'dashboard' in app.request.get('_route') %}active{% endif %}" 
                           href="{{ path('app_dashboard') }}">
                            <i class="bi bi-grid me-1"></i> Dashboard
                        </a>
                    </li>
                    {% endif %}
                    
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {% if 'admin' in app.request.get('_route') %}active{% endif %}" 
                           href="{{ path('app_admin_dashboard') }}">
                            <i class="bi bi-gear me-1"></i> Administration
                        </a>
                    </li>
                    {% endif %}
                </ul>
                
                <ul class="navbar-nav">
                    {% if app.user %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" 
                               id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                <span class="user-avatar small me-2">
                                    {{ app.user.username|first|upper }}
                                </span>
                                {{ app.user.username }}
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_profile') }}">
                                        <i class="bi bi-person me-2"></i> Mon profil
                                    </a>
                                </li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_dashboard') }}">
                                        <i class="bi bi-grid me-2"></i> Dashboard
                                    </a>
                                </li>
                                {% if is_granted('ROLE_ADMIN') %}
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item" href="{{ path('app_admin_dashboard') }}">
                                        <i class="bi bi-shield-lock me-2"></i> Administration
                                    </a>
                                </li>
                                {% endif %}
                                <li><hr class="dropdown-divider"></li>
                                <li>
                                    <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                        <i class="bi bi-box-arrow-right me-2"></i> Déconnexion
                                    </a>
                                </li>
                            </ul>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">
                                <i class="bi bi-box-arrow-in-right me-1"></i> Connexion
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-primary ms-2" href="{{ path('app_register') }}">
                                <i class="bi bi-person-plus me-1"></i> Inscription
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
    {% endblock %}
    
    {# Flash messages #}
    {% block flash_messages %}
    <div class="container mt-3">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                    {% if label == 'success' %}
                        <i class="bi bi-check-circle me-2"></i>
                    {% elseif label == 'error' or label == 'danger' %}
                        <i class="bi bi-exclamation-circle me-2"></i>
                    {% elseif label == 'warning' %}
                        <i class="bi bi-exclamation-triangle me-2"></i>
                    {% else %}
                        <i class="bi bi-info-circle me-2"></i>
                    {% endif %}
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Fermer"></button>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    {% endblock %}
    
    {# Main content #}
    <main>
        {% block body %}{% endblock %}
    </main>
    
    {# Footer #}
    {% block footer %}
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-3 mb-md-0">
                    <h5 class="mb-3">
                        <span class="me-2">📖</span> StoryForge
                    </h5>
                    <p class="text-muted small">
                        Plateforme collaborative de création narrative pour scénaristes, 
                        auteurs et game designers.
                    </p>
                </div>
                <div class="col-md-4 mb-3 mb-md-0">
                    <h6 class="mb-3">Liens utiles</h6>
                    <ul class="list-unstyled small">
                        <li><a href="{{ path('app_home') }}">Accueil</a></li>
                        {% if not app.user %}
                        <li><a href="{{ path('app_register') }}">Créer un compte</a></li>
                        <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                        {% endif %}
                    </ul>
                </div>
                <div class="col-md-4">
                    <h6 class="mb-3">Légal</h6>
                    <ul class="list-unstyled small">
                        <li><a href="{{ path('app_cgu') }}">Conditions générales d'utilisation</a></li>
                        <li><a href="{{ path('app_privacy') }}">Politique de confidentialité</a></li>
                    </ul>
                </div>
            </div>
            <hr class="my-4">
            <div class="row align-items-center">
                <div class="col-md-6 text-center text-md-start">
                    <small class="text-muted">
                        &copy; {{ "now"|date("Y") }} StoryForge - Projet TFE
                    </small>
                </div>
                <div class="col-md-6 text-center text-md-end">
                    <small class="text-muted">
                        Développé avec <i class="bi bi-heart-fill text-danger"></i> et Symfony
                    </small>
                </div>
            </div>
        </div>
    </footer>
    {% endblock %}
    
    {# Webpack Encore JS #}
    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</body>
</html>
